Modify this gamified brewing guide app so that each step has a property for image[], an array of zero or more image links that should be shown (beside each other or in a grid view) that are essential to follow the steps.

```App.tsx

```
